language: php
sudo: false

matrix:
  include:
    - php: 5.5
      env: TYPO3_VERSION=^6.2
    - php: 5.6
      env: TYPO3_VERSION=^6.2
    - php: 5.5
      env: TYPO3_VERSION=^7.6
    - php: 5.6
      env: TYPO3_VERSION=^7.6
    - php: 7
      env: TYPO3_VERSION=^7.6
    - php: 7
      env: TYPO3_VERSION=^8
    - php: 7
      env: TYPO3_VERSION=dev-master
  allow_failures:
    - env: TYPO3_VERSION=dev-master

cache:
  directories:
    - $HOME/.composer/cache

addons:
  apt:
    packages:
      - parallel

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - phpenv config-rm xdebug.ini
  - composer self-update
  - composer --version

before_script:
  # Rename our working directory, required for Extension upload to TER.
  - cd .. && mv typo3-extension-cacheopt cacheopt && cd cacheopt
  # First we install all required packages as dist versions.
  - composer require typo3/cms=$TYPO3_VERSION --prefer-dist
  # Now remove the TYPO3 core and all related symlinks.
  - rm .Build/vendor/typo3/cms -Rf
  - rm -f .Build/Web/typo3
  - rm -f .Build/Web/index.php
  # Finally we install the TYPO3 core as source version because we need some functional
  # test classes that are not included in the dist versions.
  - composer require typo3/cms=$TYPO3_VERSION --prefer-source
  - export TYPO3_PATH_WEB="$PWD/.Build/Web"
  - mkdir -p $TYPO3_PATH_WEB/typo3conf/ext
  - ln -s $PWD $TYPO3_PATH_WEB/typo3conf/ext/cacheopt

script:
  - >
    echo;
    export typo3DatabaseName="typo3";
    export typo3DatabaseHost="localhost";
    export typo3DatabaseUsername="root";
    export typo3DatabasePassword="";
    find Tests -wholename '*Tests/Functional/*Test.php' | parallel --gnu 'echo; echo "Running functional test suite {}"; ./.Build/bin/phpunit --colors -c .Build/Web/typo3/sysext/core/Build/FunctionalTests.xml {}'